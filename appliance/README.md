# Drop your appliance disk here

Put **`dune-server.vhdx`** here (from your own Windows install of the official
Dune: Awakening Self-Hosted Server, under `Virtual Hard Disks/`). It is
gitignored and never committed. This repo ships no vendor files.
